version: "3.9"
services:
  intelligence:
    build: .
    ports:
      - "8002:8002"
    environment:
      - SNAPSHOT_DB_PATH=data/snapshots.db
      - LOG_LEVEL=INFO
    volumes:
      - ./data:/app/data
